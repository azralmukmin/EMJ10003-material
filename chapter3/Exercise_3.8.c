// Exercise_3.8.c
#include <math.h>

int main(void)
{
    double u, v, w, x, y, z;

    z = sqrt(u + v) * pow(w, 2);

    z = log10(pow(x,y));

    z = sqrt(pow(x + v,2));

    return 0;
}
